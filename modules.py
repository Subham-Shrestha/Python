# Modules and Packages
# A = movies(5000){bad}      B = movies(Gindi, English, Nepali, Chinese){good}         C = movies(Hindi(Comedy,Thriller, A, D)){best}
# Module is a python file that consist code with similar functional case. It breaks large chunk of code to small files. Technically every python file is module. Empty modules also included here.
# Module creates reusable codes. CLI is fast but not user friendly while GUI is slow but user friendly.