invisible = str(input("Enter a string : "))
print(str(len(invisible)) + ' ' + 'is the length of string' + ' ' + str(invisible))


# This is updated code.
# Added comment for clarity
# The code takes a string input from the user and prints its length along with the string itself