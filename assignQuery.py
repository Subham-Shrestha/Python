# def is_even(n) :
#     if n % 2 == 0 :
#         return True
#     else :
#         return False
# a = int(input("Enter a number : "))
# print(is_even(a))

# def is_odd_even(n) :
#     if n % 2 == 0 :
#         print(f'{n} is even.')
#     else : 
#         print(f'{n} is odd.')
# a = int(input("Enter a num : "))
# is_odd_even(a)

# def find_even(n) :
#     return list(range(2, n+1, 2))
# print(find_even(10))

# write a function that takes string as parameter and returns the length of the string.
# write a function that takes string as a parameter and returns the reverse of the string.
# write a function that takes string as a parameter and checks the string is palindrome or not. Disregarding case sensitivity.
# write a function that takes multiple numbers as parameter and returns the mean of those numbers.
# write a function that takes a string as a parameter and returns the first vowel present in that string.
#  wrote a function that takes a parameter as a parameter and returns the number of vowel present in string.
# write a function that takes a parameter as string and returns the number of consonants present in string.